# 学生端和家长端均连接同一个 Render API。
VITE_API_BASE_URL=https://aixuexi-api.onrender.com
VITE_USE_MOCK_API=false

# 后端临时冷启动或AI服务异常时，允许前端使用结构化本地兜底，避免功能页面直接失败。
VITE_ALLOW_API_FALLBACK=true

# 不要在 Cloudflare 控制台手动设置 VITE_PORTAL_ROLE。
# npm run build:student 自动构建学生端；npm run build:parent 自动构建家长端。
